# Environment / secrets
.env
.env.*
!.env.sample

# Logs
*.log
logs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editors / IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Temp / runtime artifacts
*.pid
tmp/
*.tmp
.last_head_launch.sh

# Generated by start.sh from the resolved checkpoint snapshot
files/config_patched.json
files/hf_quant_config_patched.json

# Rendered launch scripts (kept for inspection, regenerated every launch)
.last_tp1_launch.sh

# Extracted verbatim from the vLLM image by start.sh, then patched in place.
# Both sides are regenerated on every launch; the generators in files/patch_*.py
# are the source of truth.
*.orig
files/mtp_patched.py
files/modelopt_patched.py
files/qsa_ops_patched.py
files/qsa_nvidia_patched.py
files/overlay/*.py
files/qsa_gb10/qsa.py

# Draft vocabularies are built from a corpus (files/build_draft_vocab.py);
# see README "Reduced-vocabulary MTP drafting".
files/draft_vocab_*.txt

# Local SpoolCache release artifact (mounted at runtime).
files/spoolcache-*.whl
